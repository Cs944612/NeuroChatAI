# The URL for the API endpoint that handles completions (use the correct server address and port)
API_URL=http://127.0.0.1:1234/v1/completions

# The model name to be used for generating completions (replace with the actual model you intend to use)
MODEL_NAME=your-model-name

# Maximum number of past messages to consider when making new predictions (useful for controlling context size)
MAX_HISTORY_MESSAGES=5

# Rate limit for API requests in seconds to avoid overloading the server (can be adjusted based on server capabilities)
RATE_LIMIT_SECONDS=1.0
